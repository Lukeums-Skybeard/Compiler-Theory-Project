#include "main.h"

int main(const int argc,
         const char *const _Nonnull *const _Nonnull argv) noexcept {
  SymbolTable symbol_table;
  Position current_position;

  // TODO

  return has_error_occurred() ? 0 : 1;
}
